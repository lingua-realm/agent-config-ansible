# Gemini CLI 环境变量
# 由 Ansible 模板渲染生成，请勿手动修改
# Gemini CLI 启动时会从 ~/.gemini/.env 加载
{% for item in agent_gemini_cli_env_normalized | dictsort %}
{{ item.0 }}={{ item.1 | string | to_json }}
{% endfor %}
