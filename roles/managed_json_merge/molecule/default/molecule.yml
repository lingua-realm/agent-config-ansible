---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: instance-managed-json-merge-default
    image: "python:3.13-bookworm"
    pre_build_image: true
provisioner:
  name: ansible
  config_options:
    defaults:
      roles_path: $PWD/../../roles
verifier:
  name: ansible