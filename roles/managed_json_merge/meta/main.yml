---
galaxy_info:
  role_name: managed_json_merge
  author: lingua-realm
  description: 将模板渲染出的 JSON/YAML 对象 deep merge 到现有 JSON 文件中，并复用 managed_file 做差异、备份与确认
  license: MIT
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: macOS
      versions:
        - all

dependencies: []